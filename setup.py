#!/usr/bin/env python

from distutils.core import setup

setup(name='PackNet',
      version='0.1',
      description='',
      author='Someone',
      author_email='none',
      packages=['packnet_sfm'],
     )
